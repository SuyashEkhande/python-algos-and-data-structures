# This is a helper custom module for "module_import.py" file

# Function to calculate sum of a list of numbers
def calculate_sum(numbers):
    return sum(numbers)

# Function to return a greeting message
def say_hello(name):
    return f"Hello, {name}!"

# Define a variable in the module
AUTHOR = "Suyash"

